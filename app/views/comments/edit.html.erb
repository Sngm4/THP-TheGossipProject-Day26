<%= form_for [@gossip, @comment] do |f|%>
  <div class='form-group'>
    <label> Content </label>
    <%= f.text_area :content, class: 'form-control' %>
</div>
  <div class='form-group'>
   <%= submit_tag "Editer le commentaire" %>  </div>
<% end %>
  <%= button_to 'Supprimer le commentaire', gossip_comment_path(@gossip.id, @comment.id), method: :delete, class: "btn btn-danger"%>
